# Dev-only YouTube OAuth credentials (each user will eventually connect their own
# Google Cloud project via an in-app wizard — this .env is a stand-in for that during
# early development). Never commit the real .env file.
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
