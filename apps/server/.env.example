# Local dev defaults — server runs on this PC for now.
# When deploying to a real host at release time, override HOST/PORT and point
# DATABASE_URL at the production Postgres instance instead of localhost.
HOST=127.0.0.1
PORT=4000
DATABASE_URL=postgresql://autouploader:autouploader@localhost:5432/autouploader
